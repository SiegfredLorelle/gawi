# Same ceiling, same reason as core/data/src/test/resources/robolectric.properties:
# the default here would be targetSdk 37, which has no android-all jar, and the
# newest that exists needs Java 21 while architecture §7 pins JDK 17. So 35 is a
# JDK constraint rather than a Robolectric one — read that file before raising it.
sdk=35
